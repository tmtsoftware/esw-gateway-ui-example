// #app-config
export const AppConfig = {
  applicationName: 'esw-gateway-ui-example'
  //Application name is used for capturing metrics of this gateway ui application. It is also used in the deployment as a subdirectory name
}
// #app-config
